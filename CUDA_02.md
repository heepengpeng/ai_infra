# CUDA 计算（Compute）体系详解

CUDA 性能优化的核心矛盾之一是"计算是否被喂饱"。这篇文档把计算这条线从最底层的算术单元，一路讲到影响计算效率的调度机制，理清楚各个概念之间的因果关系。

---

## 1. CUDA Core：最基本的算术单元

一个 SM（Streaming Multiprocessor）内部包含若干个 **CUDA Core**（部分架构文档中也称 FP32 Core），本质上是一个简单的 ALU（算术逻辑单元）。

- 每个 CUDA Core 一个时钟周期能完成一次浮点乘加（**FMA, Fused Multiply-Add**，即 `a*b+c` 算作一次操作，但计入 FLOPs 时算 2 次浮点运算）
- 一个 warp（32 个线程）执行一条算术指令时，理想情况下是 32 个 CUDA Core 同时各自处理一个线程的数据——这正是 SIMT（Single Instruction, Multiple Threads）执行模型的物理基础
- 不同架构里，一个 SM 拥有的 CUDA Core 数量并不固定等于 32。例如某些架构一个 SM 只有 16 个 FP32 单元，此时一个 warp 的指令需要拆成两个周期才能发射完，会拉低实际吞吐

### FP32 / FP64 / FP16 的算力差异

- 消费级 GPU（如 GeForce/RTX 系列）的 FP64（双精度）算力通常被大幅裁剪，往往只有 FP32 的 1/32 甚至更低，因为深度学习训练推理几乎不需要双精度
- 数据中心卡（A100 / H100 等）的 FP64 相对更强，主要面向科学计算、HPC 场景
- FP16 在 CUDA Core 上通常能达到 FP32 的 2 倍吞吐（每个 FP32 单元一个周期能处理 2 个 FP16 元素），但这还只是 CUDA Core 层面的加速，真正的跃升来自 Tensor Core

---

## 2. Tensor Core：LLM 时代真正的算力核心

Tensor Core 从 Volta 架构（V100）开始引入，是一种专用硬件单元，**只做一件事：矩阵乘加（MMA, Matrix Multiply-Accumulate）**。

### 为什么需要专用硬件做矩阵乘法

LLM 中绝大部分计算都是矩阵乘法：Linear 层的权重矩阵乘法、Attention 中的 `Q @ K^T` 和 `Attention_weights @ V`，本质上都是矩阵乘加。如果用 CUDA Core 逐元素计算，需要大量独立的乘法和加法指令来拼出一次矩阵乘；而 Tensor Core 直接在硬件电路层面实现了"一个周期完成一个小矩阵块的乘加"，效率高出一个数量级。

### 基本工作方式

Tensor Core 执行的核心操作可以抽象为：

```
D = A × B + C
```

其中 A、B、C、D 都是固定小尺寸的矩阵块（具体尺寸随架构和数据精度变化，例如某些世代是 4×4，某些是更大的分块），硬件在一个时钟周期（或极少数周期）内完成整个小矩阵块的乘加，而不是像 CUDA Core 那样一个元素一个元素地算。

实际调用 Tensor Core 通常不需要手写底层指令，而是通过：
- cuBLAS / cuDNN 等库自动调度
- 更高层的框架（PyTorch 的 AMP、CUTLASS、Triton）生成对应的矩阵乘 kernel
- 深度学习编译器在编译期识别可以用 Tensor Core 加速的算子

### 精度与算力的关系（关键中的关键）

Tensor Core 支持的数值精度越低，单位时间内能处理的矩阵元素就越多，理论算力（FLOPS/OPS）就越高。大致规律（以近几代数据中心架构为参考，具体数字因代际不同会变化，但相对倍数关系稳定）：

| 精度 | 相对算力（近似） | 典型用途 |
|---|---|---|
| FP32（走 CUDA Core，非 Tensor Core） | 基准 1x | 传统通用计算 |
| TF32 | 约 4–8x | 训练（Ampere 起，牺牲少量尾数精度换速度，对用户几乎透明） |
| FP16 / BF16 | 约 8–16x | 训练主力精度，BF16 动态范围更接近 FP32，更适合大模型训练稳定性 |
| INT8 | 约 16–32x | 推理量化的主流选择 |
| INT4 / FP4 | 更高（较新架构支持） | 极致推理量化，牺牲精度换吞吐和显存 |

**这就是为什么量化（Quantization）能大幅提升推理速度**——量化不仅仅是为了省显存，更重要的是把计算从低倍率的精度路径切换到了 Tensor Core 支持的高吞吐路径。理解这张表，是理解后续 INT8/INT4 推理优化工作的地基。

---

## 3. 算力峰值怎么算出来的

理论峰值 FLOPS（每秒浮点运算次数）的计算公式：

$$
\text{峰值 FLOPS} = \text{SM 数量} \times \text{每 SM 每周期的运算次数} \times \text{时钟频率} \times 2
$$

其中乘以 2 是因为一次 FMA（乘加融合）被计为 2 次浮点操作（一次乘法 + 一次加法）。

**举例**：假设某 GPU 有 108 个 SM，每个 SM 每周期能执行 64 次 FP32 FMA，主频 1.4GHz：

$$
108 \times 64 \times 1.4\times10^9 \times 2 \approx 19.4 \text{ TFLOPS（FP32 理论峰值）}
$$

Tensor Core 的峰值算力算法逻辑类似，但基本单位从"标量 FMA"换成了"矩阵块 MMA"，因此同一块芯片在 Tensor Core 路径下的理论峰值会比纯 CUDA Core 高出一到两个数量级。

### 理论峰值 vs 实际跑到的算力：MFU

**理论峰值是天花板，几乎不可能真正跑满。** 实际衡量一个 kernel 或一次训练/推理任务优化水平的指标是：

$$
\text{MFU (Model FLOPs Utilization)} = \frac{\text{实际达到的 FLOPS}}{\text{理论峰值 FLOPS}}
$$

训练大模型时常听到"MFU 打到 40%-50% 已经算优化得不错"，说的就是这个概念。MFU 上不去的原因往往不在计算单元本身，而在于：
- 内存带宽跟不上（memory-bound，见下一节要讲的内存体系）
- Warp/Occupancy 相关的调度效率问题（本文第 4 节）
- kernel launch 开销、算子之间的空隙（可以通过算子融合、CUDA Graph 缓解）

---

## 4. Warp 级别的计算效率问题

硬件算力再强，如果指令调度和执行方式不对，也发挥不出来。这一层关注的是"算力单元有没有被真正喂饱"。

### Warp Divergence（分支分化）

```cuda
if (threadIdx.x % 2 == 0) {
    // 分支 A
} else {
    // 分支 B
}
```

同一个 warp 内的 32 个线程如果因为条件判断走上了不同分支，硬件的处理方式是**先执行分支 A（此时走分支 B 的线程被屏蔽、处于空闲状态），再执行分支 B（走分支 A 的线程空闲）**。也就是说，这个 warp 原本一个周期能完成的工作，现在要拆成多个周期串行完成，相当于这部分算力被打了折扣。

- 分支越多、线程间走向越不规则，效率损失越大
- 优化思路通常是让同一个 warp 内的线程尽量走相同分支（比如按 warp 边界对齐数据、用数学运算替代条件判断）

### 指令级并行与延迟隐藏

- 一个 SM 内有多个 **warp 调度器（warp scheduler）**，可以在同一周期内从不同 warp 中选取已就绪的指令发射执行
- 当某个 warp 正在等待前一条指令的结果（比如除法、内存读取这类延迟较长的操作）时，调度器会切换到另一个已就绪的 warp 继续执行，而不是干等
- 这是 GPU"用大规模并行掩盖单条指令延迟"的核心设计哲学，和内存访问延迟隐藏（下一篇要讲的内容）是同一套思路，只是这里隐藏的是**计算/流水线延迟**

### Occupancy 如何影响计算效率

一个 SM 能同时驻留（resident）的 warp 数量越多，warp 调度器可供切换的"备选项"就越多，越容易在某个 warp 阻塞时找到别的 warp 填补空闲周期，从而让计算单元持续保持忙碌状态。

这个"一个 SM 同时能驻留多少个 warp/block"，就是 **Occupancy（占用率）** 概念，它同时受三个资源上限的约束：
1. 寄存器用量（每个线程用的寄存器越多，同时能驻留的线程就越少）
2. Shared Memory 用量（每个 block 占用的 shared memory 越多，同时能驻留的 block 就越少）
3. Block Size 本身（block 内线程数如何切分成 warp，是否有浪费）

Occupancy 不是越高越好，但过低的 Occupancy 通常意味着 SM 有大量空闲周期没有 warp 可调度，是排查"为什么这个 kernel 算力利用率上不去"的第一个要看的指标。

---

## 5. 小结：计算这条线的因果链

```
CUDA Core（标量计算）
  → Tensor Core（矩阵乘加，LLM 算力主力）
      → 精度选择决定 Tensor Core 吞吐上限（FP32→TF32→FP16/BF16→INT8→INT4，逐级提速）
          → 理论峰值 FLOPS 是天花板，MFU 衡量实际利用率
              → Warp Divergence 和 Occupancy 决定这些算力单元是否被真正喂饱
```

这条链条解释了从"硬件有多少算力"到"实际任务能吃到多少算力"之间的完整过程，也是理解后续量化、算子融合、FlashAttention 等推理优化技术为什么有效的基础。

---

*下一篇：内存（Memory）体系——内存层次结构、Memory Coalescing、Shared Memory 手动管理，以及连接计算与内存两条线的 Roofline Model。*
